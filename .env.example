# =============================================
# Ent-DNS Enterprise — Environment Variables
# Copy this file to .env and customize values.
# NEVER commit .env to version control.
# =============================================

# --- Database ---
# Path to the SQLite database file (inside the container volume)
ENT_DNS__DATABASE__PATH=/data/ent-dns/ent-dns.db

# --- DNS Server ---
ENT_DNS__DNS__BIND=0.0.0.0
ENT_DNS__DNS__PORT=53
# Comma-separated upstream resolvers (DoH URLs or plain IP:port)
# ENT_DNS__DNS__UPSTREAMS=https://1.1.1.1/dns-query,https://8.8.8.8/dns-query

# --- API Server ---
ENT_DNS__API__BIND=0.0.0.0
ENT_DNS__API__PORT=8080

# --- Authentication ---
# REQUIRED: Change this to a strong random value in production!
# Generate one with: openssl rand -hex 32
ENT_DNS__AUTH__JWT_SECRET=change-me-in-production-use-openssl-rand-hex-32

# JWT token lifetime in hours (default: 24)
ENT_DNS__AUTH__JWT_EXPIRY_HOURS=24
